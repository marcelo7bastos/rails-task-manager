<h1> SHOW Html ERB </h1>
<h1> Detais tasks</h1>

<ul>
  <li><%= @task.title %></li>
  <li><%= @task.details %></li>
  <li><%= @task.completed %></li>
</ul>
<p><%= link_to 'Edit Task', edit_task_path(@task.id) %></p>
<p><%= link_to 'Show all Task', tasks_path %></p>
<p><%= link_to 'Create a new Task', new_task_path %></p>

<%# DELETE  %>
<%= link_to "Delete #{@task.title}",
            task_path(@task),
            method: :delete,
            data: { confirm: "Are you sure?" } %>
